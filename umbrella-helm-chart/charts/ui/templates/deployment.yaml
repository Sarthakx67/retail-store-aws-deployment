apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui   
  labels:
    app: ui              
    environment: {{ .Values.environment }}
spec:        
  selector:
    matchLabels:
      app: ui            
      component: frontend
  template:
    metadata:
      labels:
        app: ui          
        component: frontend
    spec:
      containers:
      - name: ui
        image: 
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: orders-configmap  
        resources: