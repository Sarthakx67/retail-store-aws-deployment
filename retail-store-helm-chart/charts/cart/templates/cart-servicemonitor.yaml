# ServiceMonitor = a Prometheus Operator resource that tells Prometheus which services to scrape and how.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cart-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: cart
      component: backend
  namespaceSelector:
    matchNames:
      - retail-store-{{ .Values.global.environment }}
  endpoints:
  - port: cart
    path: /actuator/prometheus
    interval: 15s

