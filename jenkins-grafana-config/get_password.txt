kubectl exec -n jenkins -it deploy/jenkins -- cat /var/jenkins_home/secrets/initialAdminPassword

#backup jenkins jenkins_home
kubectl cp jenkins/jenkins-pod:/var/jenkins_home ./jenkins-backup

#Restore later:
kubectl cp ./jenkins-backup jenkins/jenkins-pod:/var/jenkins_home
